<?xml version='1.0' encoding='utf-8'?>
<widget id="com.manishgupta.coindrivex" version="1.0.0"
    xmlns="http://www.apache.org/ns/widgets"
    xmlns:cdv="http://cordova.apache.org/ns/1.0">

    <name>CoinDriveX</name>
    <description>CoinDriveX - 3D Heist Racing Game</description>
    <author email="your@email.com" href="https://manishgupta290397-sys.github.io/coindrivex">
        Manish Gupta
    </author>

    <!-- Load from local www folder — fully offline -->
    <content src="index.html" />

    <!-- Allow all network requests (AdMob + JSONBin leaderboard) -->
    <access origin="*" />
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-navigation href="*" />

    <!-- Android settings -->
    <platform name="android">
        <allow-intent href="market:*" />

        <preference name="Fullscreen" value="true" />
        <preference name="Orientation" value="portrait" />

        <!-- AdMob App ID -->
        <config-file parent="/manifest/application" target="AndroidManifest.xml">
            <meta-data
                android:name="com.google.android.gms.ads.APPLICATION_ID"
                android:value="ca-app-pub-2765601211878225~6409661215" />
        </config-file>

        <!-- Permissions -->
        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
        <uses-permission android:name="android.permission.WAKE_LOCK" />
    </platform>

    <!-- Performance settings -->
    <preference name="webviewbounce" value="false" />
    <preference name="UIWebViewBounce" value="false" />
    <preference name="DisallowOverscroll" value="true" />
    <preference name="BackgroundColor" value="0xff000000" />
    <preference name="SplashScreenDelay" value="2000" />
    <preference name="AndroidLaunchMode" value="singleTop" />

    <!-- Hardware acceleration for WebGL/Three.js -->
    <preference name="android-hardwareAccelerated" value="true" />

    <!-- Plugins -->
    <plugin name="admob-plus-cordova" spec="^1.25.0" />
    <plugin name="cordova-plugin-whitelist" spec="^1.3.4" />
    <plugin name="cordova-plugin-splashscreen" spec="^6.0.0" />
    <plugin name="cordova-plugin-statusbar" spec="^3.0.0" />

</widget>
